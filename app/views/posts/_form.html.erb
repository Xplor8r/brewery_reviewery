<%= form_for [@brewery_thread, @post] do |f| %>

  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h5><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h5>

      <ul>
        <% @post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.text_area :body, placeholder: "Post a comment", rows: 2, class: "form-control", data: { behavior: "comment-body" } %>
  </div>
  <div class="form-group">
    <%= f.submit "#{"Update" unless f.object.new_record?} Comment", class: "btn btn-primary"%>
  </div>
     
<% end %>