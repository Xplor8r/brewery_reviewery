<p><small><%= link_to "Back to the thread", brewery_thread_path(@brewery_thread) %></small></p>

<p class="thread-details">
  <strong><%= brewery_state_link(@brewery_thread.brewery_state) %></strong>
  • Posted <%= time_ago_in_words(@brewery_thread.created_at) + ' ago' %> by <%= @brewery_thread.user.name %>
</p>

<br />

<%= content_tag :div, id: dom_id(@post) do %>
  <div class="card-header">
    <div>
      <strong class="post-user"><%= @post.user.name %></strong>
      <small> commented on <%= link_to @post.created_at.strftime("%b %d, %Y"), brewery_thread_url(@brewery_thread, anchor: "post_#{@post.id}") %>:</small>
    </div>
  </div>

  <div class="card-body">
    <%= render "form" %>
  </div>
<% end %>